<%= form_with url: locations_path(params[:trip_id]),
              method: :get,
              html: {
                class: "mb-4",
                data: {
                  autocomplete_target: "form"
                }
              } do |f| %>

  <div class="input-icons r13 w-50">
    <i class="fa-solid fa-magnifying-glass icon"></i>
    <div class="d-flex justify-content-between">
      <%= f.text_field :query,
                        class: "form-control r13 input-field",
                        value: params[:query],
                        placeholder: "Search for a location",
                        data: {
                        autocomplete_target: "input r13",
                        action: "keyup->autocomplete#update"
                        } %>
      <%= f.submit "Search", class:"input-btn r13" %>
    </div>
  </div>
<% end %>
